
新增文件说明：
main_multhreds.cc:多线程部分主函数
sudokumul.h：多线程部分的头文件
sudokumul_dancing_link.cc:dancing_link算法的多线程版本（主要将输出从全局变量改为函数参数）

代码执行说明：
 1.make
 2.可执行程序 sudoku:原代码版本 
	sudoku_slove:多线程版本，线程个数会动态检测目标机器上核数目进行分配。（即实验要求版本）
 
运行说明:
./sudoku_slove便可执行，输出结果打印到屏幕，也可重定向输出到文件中，例如./sudoku_slove > output1
程序运行后可以输入输入文件名，输入文件可输入多个，程序输入EOF停止读入。
注：默认不显示时间，如果需要显示原代码时间需打开sudoku.h修改const bool DEBUG_TIME 为true，如果需要显示多线程版本花费时间需打开sudokumul.h修改const bool DEBUG_MODE 为 true;

 详细说明见代码
 
